{
  "name": "jupyter-js-output-area",
  "version": "0.0.1",
  "description": "Library for handling and displaying Jupyter output messages.",
  "main": "lib/output-area.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf ./es6-lib && mkdirp ./es6-lib && rimraf ./es5-lib && mkdirp ./es5-lib && rimraf ./dist && mkdirp ./dist",
    "build": "npm run build:ts && npm run build:es5 && npm run build:browser",
    "build:ts": "tsc",
    "build:es5": "babel es6-lib/ --out-dir es5-lib/ --source-maps",
    "build:browser": "browserify es5-lib/*.js --outfile dist/jupyter-js-output-area.js",
    "prepublish": "npm run build",
    "watch": "npm run watch:build -s",
    "watch:build": "nodemon -q -w src/ --ext '.' --exec 'npm run build'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jupyter/jupyter-js-output-area.git"
  },
  "keywords": [
    "jupyter",
    "js",
    "frontend",
    "front",
    "end",
    "output",
    "area",
    "outputarea",
    "display",
    "mimetype",
    "mime",
    "notebook",
    "component",
    "message",
    "msg"
  ],
  "author": "Project Jupyter",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/jupyter/jupyter-js-output-area/issues"
  },
  "homepage": "https://github.com/jupyter/jupyter-js-output-area#readme",
  "dependencies": {
    "transformime": "^2.0.0",
    "transformime-jupyter-transformers": "^0.1.0"
  },
  "devDependencies": {
    "babel": "^5.8.20",
    "browserify": "^11.0.1",
    "mkdirp": "^0.5.1",
    "nodemon": "^1.4.0",
    "rimraf": "^2.4.2",
    "typescript": "^1.5.3"
  }
}
